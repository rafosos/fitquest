export default class Rotina{
    constructor(id: number, nome: string, dias: number) {
        this.id = id;
        this.nome = nome;
        this.dias = dias;
    }

    id: number;
    nome: string;
    dias: number;
}